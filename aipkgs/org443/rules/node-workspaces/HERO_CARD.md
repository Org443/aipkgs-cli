# node-workspaces

Workspace rules for an npm-workspaces Node monorepo.

**Covers**
- Workspace scopes (`@service/*`, `@local/*`) and their build contracts
- Where dependencies live: hoisted root vs workspace-local
- Cross-workspace import boundaries

**Pairs with:** `node-general` for general Node + TypeScript coding rules.
