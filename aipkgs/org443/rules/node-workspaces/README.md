# node-workspaces

_A Claude Code rule._

Node + TypeScript rules for an npm-workspaces monorepo: workspace scopes, build contracts, dependency placement.

## Install

```sh
npx @aipkgs/cli install org443/node-workspaces
```
